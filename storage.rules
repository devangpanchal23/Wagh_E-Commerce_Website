rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Per-user profile image storage rules: only owner can read and write
    match /profileImages/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
